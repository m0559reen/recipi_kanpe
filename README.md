# レシピカンペ（カクテルレシピ帳 PWA）

単一の `index.html`（ビルド不要）＋ Service Worker の PWA です。レシピは端末内の IndexedDB に保存されます。

## ファイル構成

```
index.html              アプリ本体（HTML/CSS/JS すべて入り）
manifest.webmanifest    PWA 定義
sw.js                   オフライン用 Service Worker
icons/                  アイコン 4 種
```

## GitHub Pages で公開する

1. GitHub で新しいリポジトリを作成する（例: `recipe-kanpe`）。
2. このフォルダの中身（`index.html` など。フォルダごとではなく **中身**）をリポジトリ直下にアップロードする。
3. リポジトリの **Settings → Pages** を開き、Source を「Deploy from a branch」、Branch を `main` / `/ (root)` にして Save。
4. 数分後、`https://<ユーザー名>.github.io/recipe-kanpe/` で開けます。

パスはすべて相対指定なので、サブパス公開（`/recipe-kanpe/`）でもそのまま動きます。HTTPS 必須ですが GitHub Pages は標準で HTTPS です。

## スマホに入れる

- iPhone: Safari で開く → 共有 → **ホーム画面に追加**
- Android: Chrome で開く → メニュー → **アプリをインストール**（または「ホーム画面に追加」）

一度開けばオフラインでも使えます。

## データの扱い（重要）

- レシピは **その端末・そのブラウザの中だけ** に保存されます。端末どうしの自動同期はありません。
- 機種変更や端末故障に備えて、メニュー（右上 ⋯）の **バックアップを書き出す** で JSON を定期的に保存してください。**バックアップから復元** で読み込めます。
- iPhone の Safari は、ホーム画面に追加していない状態だと、長期間開かないとデータが消えることがあります。ホーム画面に追加して使うのがおすすめです。

## アプリを更新して再公開するとき

`index.html` を差し替えたら、`sw.js` の先頭近くにある

```js
const CACHE = 'recipe-kanpe-v3';
```

の番号を上げて（今回の版は `v3`、次は `v4`…）からアップロードしてください。古いキャッシュが自動で消えます（保存済みのレシピには影響しません）。

アップロード後、**ページを2回再読み込み**すると新しい版になります（1回目で新しい版を取得し、2回目から表示されます）。

## 更新履歴

- v1.2.0: ページ下部に「↑ 先頭へ」ボタンを追加（一定量スクロールすると表示） / シェイカー・グラスのアイコンを、技法からの自動表示だけでなく編集画面で直接オン・オフできるように変更（既存レシピは今まで通りの見た目のまま、明示的な設定に自動移行）
- v1.1.0: 「ゲージ」表記を「目盛り」に変更 / 一覧を1列表示に変更（1レシピを大きく表示） / ★オリジナルを廃止し、味わいの自由タグを追加（★付きだった既存レシピは自動で「オリジナル」タグに移行）
- v1.0.0: 初版

## 使い方メモ

- **量の入力**: `30`（ml）、`1tsp`、`fill`、`1dash`、`1/6` など自由入力。ml と tsp は 5ml = 1マスの目盛りになります。
- **目盛りの色**: 材料の並び順で「濃灰 → 白 → 薄灰 → 白…」と自動で塗り分けます（元の PDF と同じ規則）。
- **目盛り** のチェックをはずすと文字だけの表示に、**別枠** にすると目盛りの外に小さな1マスで表示します（例: ミントカミカゼの「ミントリキュール 1tsp（沈める）」）。
- **シェイカー・グラスのアイコン**: 編集画面の「シェイカー・グラスの表示」で、シェイカー／グラス／グラス（炭酸）をオン・オフできます。技法（ビルド・シェイクなど）を選ぶと自動で候補が入りますが、手動で切り替えるとその内容がそのまま保存され、以後は技法を変えても自動では変わりません。
- **味わいタグ**: 自由に付けられるタグ（フルーティ・ドライ・オリジナル など）。編集画面で候補から選ぶか、入力して追加します。一覧では名前の下に表示され、絞り込みにも使えます。
- **絞り込み**: スピリッツ / 技法 / 味わい（いずれも複数選択可・選んだもののいずれかに一致）/ 副材料・割り材（「すべて含む」「いずれか含む」を切替）。検索欄はレシピ名・材料・メモを、ひらがなでも検索できます。
- **先頭へ戻る**: 画面を下にスクロールすると、左下に「↑ 先頭へ」ボタンが出ます。タップすると検索・絞り込みのある画面上部に戻ります。
- **印刷 / PDF保存**: メニューから。検索・絞り込みの UI は隠れ、レシピ部分だけが出力されます。
